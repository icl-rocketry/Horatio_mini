function [h] = equality_constraint_fn()
% Defines safe track for rocket and returns value if rocket passes off the track.
    h = 0; % Currently defined as zero as no rail needed for rocket path.
end

